# خطة تطوير إدارة الحسابات — مستشفى ميتالي

## القرار الأمني المطلوب قبل التنفيذ

الطلب بإظهار كلمة المرور الحالية للمدير وتخزينها كنص صريح في Firestore يتعارض مع متطلبات الأمان الأساسية؛ إذ إن أي نسخة احتياطية أو صلاحية قراءة خاطئة ستكشف دخول جميع الموظفين. لذلك لن تُخزّن كلمات المرور كنص صريح، ولن يمكن استرجاع كلمة المرور القديمة. البديل الآمن يحافظ على تجربة Soli Medical: يستطيع المدير كتابة كلمة مرور مؤقتة جديدة أو تعيين كلمة مرور جديدة، مع زر إظهار/إخفاء أثناء الإدخال، لكن لا يستطيع قراءة الكلمة الأصلية بعد حفظها.

## النموذج المقترح

سيُستخدم مسار مصادقة خادمي آمن بدل الاعتماد على مقارنة كلمة المرور داخل المتصفح. تُحفظ في Firestore قيمة hash قوية فقط، مثل Argon2id أو bcrypt، مع salt مستقل لكل حساب. يتحقق خادم Vercel من كلمة المرور الأخيرة، ثم يصدر جلسة قصيرة العمر. تبقى بيانات الاسم واسم الدخول والدور والحالة والصلاحيات التفصيلية وإصدار الأمان في ملف الحساب. لن يقرأ الموظف صلاحياته للكتابة، ولن يستطيع تعديل ملفه أو صلاحياته.

للحفاظ على الأجهزة المعتمدة، سيُحفظ اعتماد جهاز عشوائي قابل للإبطال، وليس كلمة المرور. تغيير كلمة المرور أو إيقاف الحساب يزيد `securityVersion` ويُلغي اعتماد الأجهزة عند أول اتصال. لا يمكن ضمان معرفة التغيير أثناء انقطاع الإنترنت؛ لذلك سيستمر الجهاز المعتمد بالعمل حتى انتهاء مدة الاعتماد أو عودته للاتصال، ثم يُرفض فورًا إذا كان الإصدار قد تغيّر.

## الملفات التي ستتغير

| الملف | التغيير المقترح |
|---|---|
| `index.html` | إعادة تصميم بطاقة إدارة المستخدمين لتعرض الاسم، اسم الدخول، حقل كلمة مرور جديد مع زر إظهار/إخفاء، الدور، الحالة، والصلاحيات. لن تعرض كلمة المرور المحفوظة أو عبارة Firebase Authentication داخل الجدول. ستنتظر الواجهة نتيجة الحفظ قبل إظهار نجاح، وستمنع الموظف من رؤية صفحة الإدارة أو تعديل صلاحياته. |
| `firebase-store.js` | إضافة واجهة طلبات المصادقة الآمنة لإنشاء الحساب وتغيير كلمة المرور وتعيين الحالة والصلاحيات وإصدار الأمان. لن تُرسل كلمة المرور إلى سجلات التدقيق ولن تُعاد إلى الواجهة بعد الحفظ. |
| `firestore.rules` | منع القراءة العامة لكلمات المرور أو hash، وقصر كتابة ملفات الحسابات والصلاحيات على المدير. يُسمح للمستخدم بقراءة ملفه الضروري فقط، ولا يُسمح له بتغيير الدور أو الحالة أو الصلاحيات أو إصدار الأمان. |
| `vercel.json` | إضافة إعدادات مسارات API إذا تم اختيار المصادقة الخادمية عبر Vercel Functions. |
| `api/auth/*` أو مسارات خادمية مكافئة | التحقق من كلمات المرور باستخدام hash، إصدار الجلسات، إبطال الجلسات، وتطبيق حظر المحاولات المتكررة دون كشف أسرار. |
| `tools/security-auth-tests.js` | إضافة اختبارات إنشاء موظف، الدخول من جهاز ثانٍ، تغيير كلمة المرور، رفض القديمة، قبول الجديدة، إيقاف الحساب، ومنع الموظف من تعديل الصلاحيات. |
| `sw.js` ونسخة التطبيق | تحديث ذاكرة Service Worker بعد اجتياز الاختبارات فقط. |

## ما لن يتغير

لن تُحذف أي مجموعة أو وثيقة من Firestore. ستبقى أسماء المستخدمين والأدوار والصلاحيات الحالية، وسيتم ترحيل الحسابات الحالية بإضافة الحقول الناقصة فقط. لن تتغير بيانات المرضى أو الزيارات أو الإيرادات أو المصروفات أو الأطباء أو سجل النشاط.

## خطة الانتقال غير المدمرة

أولًا، سيُؤخذ backup قراءة فقط من الحسابات الحالية للتحقق من الحقول والربط. ثانيًا، ستُضاف حقول `passwordHash` أو مرجع المصادقة، `status`, `permissions`, `securityVersion`, `lastPasswordChangeAt`, و`trustedDevices` للحسابات الناقصة دون حذف الحقول القديمة. ثالثًا، سيُفعّل مسار القراءة القديم كخطة رجوع مؤقتة فقط للحسابات التي لم تُرحّل، مع منع إنشاء حسابات جديدة عليه. رابعًا، سيُختبر موظف تجريبي منفصل من جهازين. خامسًا، بعد نجاح الاختبارات فقط يُنشر الإصدار ويُترك backup محفوظًا.

## الاختبارات المطلوبة

سيُختبر إنشاء موظف جديد وحفظه، ثم تسجيل دخوله من متصفح ثانٍ باستخدام كلمة المرور الجديدة. بعد ذلك سيُغيّر المدير كلمة المرور، ويُختبر رفض الكلمة القديمة وقبول الجديدة، ثم إيقاف الموظف والتأكد من رفض دخوله بعد مزامنة الإصدار. كما سيُختبر أن الموظف لا يرى زر إدارة المستخدمين ولا يستطيع إرسال تعديل للصلاحيات، وأن رسائل النجاح لا تظهر قبل تأكيد Firestore أو الخادم.

## القرار المطلوب

الخيار الآمن هو: **إظهار/إخفاء كلمة المرور أثناء تعيين كلمة مرور جديدة فقط، وعدم عرض كلمة المرور الحالية بعد حفظها**. إذا كان هذا مقبولًا، يبدأ التنفيذ دون حذف بيانات. أما تنفيذ كلمة مرور نصية قابلة للعرض من Firestore فلا أوصي به ولن أنفذه لأنه يعرّض حسابات الموظفين وبيانات المستشفى للخطر.
