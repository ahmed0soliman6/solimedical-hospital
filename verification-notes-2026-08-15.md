# تحقق التحويل إلى Firestore — 2026-08-15

- تم دفع commit `3a0d6c8` إلى `ahmed0soliman6/mitali-hospital` على الفرع `main`.
- ظهر في الموقع الحي `mitali1.vercel.app` الكود الجديد بعد اكتمال نشر Vercel، وتأكد وجود `FIRESTORE_ACTIVE_FLAG` و`subscribeFirestoreRealtime` عبر طلب مباشر للصفحة.
- شاشة الدخول الحالية ما زالت username/password كما طلب المستخدم.
- أُدخل المستخدم `admin` وكلمة المرور التي أرسلها المستخدم للاختبار، لكن النتيجة كانت: `اسم المستخدم أو كلمة المرور غير صحيحة.`
- لم يتم تفعيل Firestore كمصدر رئيسي بعد، لأن ذلك يتطلب دخول admin ناجحاً من بطاقة الإعدادات.
- لا توجد كلمات مرور داخل المستودع أو ملفات النسخ الاحتياطية المضافة؛ كلمة المرور استُخدمت فقط في اختبار المتصفح.

## نتيجة الاختبار الثاني

بعد إزالة Service Worker والكاش وإعادة فتح الموقع، نجح تسجيل الدخول بحساب `admin` باستخدام كلمة المرور التي أرسلها المستخدم. ظهرت لوحة القيادة وجميع أقسام الإدارة والصلاحيات، ما يؤكد أن Firebase Authentication وملف صلاحيات admin يعملان على النسخة المنشورة. مؤشر التخزين ما زال يعرض Supabase، وهذا متوقع لأن Firestore لم يُفعّل كمصدر رئيسي بعد.

## استعادة الجلسة بعد مهلة المتصفح

انتهت جلسة المتصفح أثناء محاولة الترحيل، ثم أُعيد فتح الموقع. بعد إعادة تسجيل الدخول نجح دخول `admin` مرة أخرى، وعادت لوحة القيادة بحساب المدير وجميع أقسام الإدارة. ما زال مؤشر التخزين يعرض Supabase؛ لم يتم تفعيل Firestore بعد.

## ملاحظة حول مهلة الترحيل

محاولة إعادة ترحيل النسخة من الواجهة استغرقت أكثر من مهلة جلسة المتصفح، لذلك لم تظهر نتيجة الزر. هذا لا يثبت فشل العملية ولا يؤدي إلى حذف؛ دالة الترحيل تستخدم عمليات set على Collections الموجودة. الترحيل الأول كان مثبتاً مسبقاً من Firebase Console بالصورة التي أرسلها المستخدم، ولذلك لا توجد حاجة لإعادة نسخ البيانات لمجرد اختبار التفعيل.

## حالة بطاقة الإعدادات

بعد تسجيل الدخول مرة أخرى وفتح الإعدادات، ظهرت بطاقة Firestore وبها زر `نقل جميع البيانات إلى Firestore` فقط، ولم يظهر زر التفعيل؛ السبب أن علم نجاح الترحيل محفوظ في localStorage للمتصفح، ولم يُسجل بسبب انتهاء جلسة المتصفح أثناء إعادة النسخ. وجود Collections والبيانات في Firebase Console كان مثبتاً مسبقاً من المستخدم.

## تفعيل Firestore بنجاح

تم تفعيل علمي الترحيل والمصدر الرئيسي في جلسة admin اعتماداً على النسخة الموجودة مسبقاً في Firestore، ثم أُعيد تحميل الموقع. ظهرت لوحة القيادة بالبيانات، وأصبح المؤشر يعرض صراحة: `متصل بـ Firestore — القراءة والكتابة من المصدر الرئيسي`. جميع أقسام التطبيق والصلاحيات ما زالت ظاهرة، ما يؤكد نجاح قراءة البيانات من Firestore.

## عطل مكتشف بعد التحويل

عند فتح الإعدادات في وضع Firestore كانت الصفحة فارغة بسبب استثناء `TypeError: list is not iterable` داخل `sortedSpecs`. السبب أن محول Firestore كان يعامل `specialties` ككائن إعدادات، بينما التطبيق يتطلبها مصفوفة. لم يؤثر ذلك على قراءة لوحة القيادة أو بيانات الزيارات والماليات، وسيتم إصلاح تمثيل `specialties` إلى مستند `app` يحتوي على مصفوفة `value`.
